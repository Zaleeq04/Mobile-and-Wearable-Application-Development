<?xml version="1.0" encoding="utf-8"?>

<!-- Root layout container for match suggestion screen -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main vertical layout for content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Screen heading for smart match suggestions -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="Possible Matches"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Button to return user to homepage -->
        <Button
            android:id="@+id/backHomeBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Back to Home" />

        <!-- RecyclerView displays matching found item reports -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/matchRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>